# Node 10 with build tools for node-sass compilation
FROM node:10-alpine

# Install build tools required for native modules like node-sass
RUN apk add --no-cache \
    python2 \
    make \
    g++ \
    git

# Set working directory
WORKDIR /var/www/html

# Install gulp globally
RUN npm install -g gulp
