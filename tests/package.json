{
  "name": "abr-e2e-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vitest run --project e2e",
    "test:api": "vitest run --project api",
    "test:api:watch": "vitest --project api",
    "test:no-mocks": "USE_MOCKS=false vitest run",
    "cleanup": "./e2e/fixtures/cleanup-test-data.sh"
  },
  "devDependencies": {
    "agent-browser": "^0.13.0",
    "vitest": "^2.0.0",
    "zod": "^3.23.0"
  }
}
